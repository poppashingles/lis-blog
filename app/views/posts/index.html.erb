<h1>Nananananaa, it's the motherfuckin' L-I Li</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, post, class: "specialfont" %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <% if false %>
        <td><%= link_to 'Delete', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'This way to the lab with a pen and a pad', new_post_path, class: "btn" %>